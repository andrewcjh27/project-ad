# Core (required)
pillow>=10.0
numpy>=1.24
scikit-learn>=1.3

# Optional — real model backends (enable by setting the matching API key)
# openai>=1.0          # OPENAI_API_KEY  -> gpt-image-1 (images) + gpt-4o-mini (LLM)
# anthropic>=0.40      # ANTHROPIC_API_KEY -> Claude (LLM)
# replicate>=0.34      # REPLICATE_API_TOKEN -> Flux 1.1 Pro (images)
# requests>=2.31       # used by the Replicate image path
